<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.Mapper.TimeRecordMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO time_record(employee_id, task_id, start_time, end_time, description)
        VALUES (#{employeeId}, #{taskId}, #{startTime}, #{endTime}, #{description})
    </insert>

    <select id="findByEmployeeAndPeriod" resultType="org.example.Entity.TimeRecord">
        SELECT *
        FROM time_record
        WHERE employee_id = #{employeeId}
        AND start_time &gt;= #{start}
        AND end_time &lt;= #{end}
    </select>

</mapper>